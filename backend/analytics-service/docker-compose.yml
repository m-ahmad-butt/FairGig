services:
  analytics-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: analytics-service-local
    env_file:
      - .env
    ports:
      - '8001:8001'
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    container_name: analytics-service-mongo-local
    ports:
      - '0:27017'
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
