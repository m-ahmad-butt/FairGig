services:
  grievance-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grievance-service-local
    env_file:
      - .env
    ports:
      - '4003:4003'
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    container_name: grievance-service-mongo-local
    ports:
      - '0:27017'
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
